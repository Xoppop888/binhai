import { useState } from 'react';
import { SiteContacts, whatsappUrl } from '../data/siteSettings';
export default function ContactWidget({ contacts }: { contacts: SiteContacts }) { const [open, setOpen] = useState(false); const [showWeChat, setShowWeChat] = useState(false); return <><div className="contact-fab-wrap">{open && <div className="contact-menu"><a href={contacts.telegram} target="_blank" rel="noreferrer" style={{ background:'#229ED9' }}>Telegram ↗</a><a href={whatsappUrl(contacts.whatsapp)} target="_blank" rel="noreferrer" style={{ background:'#25D366' }}>WhatsApp ↗</a><button onClick={() => setShowWeChat(true)} style={{ background:'#07C160' }}>WeChat</button></div>}<button className="contact-fab" onClick={() => setOpen((v) => !v)}>{open ? '× Закрыть' : 'Связаться ↗'}</button></div>{showWeChat && <div className="modal-backdrop" onClick={() => setShowWeChat(false)}><div className="modal" style={{ maxWidth:360,textAlign:'center' }} onClick={(e) => e.stopPropagation()}><h3>Мы в WeChat</h3><p>Добавьте нас по ID:</p><img src={`https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${contacts.wechat}`} alt="WeChat QR" width="180" height="180" /><p><strong>{contacts.wechat}</strong></p><button className="page-button" onClick={() => setShowWeChat(false)}>Закрыть</button></div></div>}</>; }
