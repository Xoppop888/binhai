import { FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { supabase, isSupabaseConfigured } from '../lib/supabaseClient';
export default function AdminLogin(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const navigate=useNavigate();async function submit(e:FormEvent){e.preventDefault();if(!supabase)return;setLoading(true);setError('');const {error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error){setError('Неверный email или пароль');return}navigate('/admin')}if(!isSupabaseConfigured)return <div className="admin-page"><div className="admin-login"><span className="admin-kicker">BINHAI AUTO / CONTROL ROOM</span><h1>Supabase не настроен</h1><p>Добавьте VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY в GitHub Secrets.</p></div></div>;return <div className="admin-page"><form className="admin-login" onSubmit={submit}><span className="admin-kicker">BINHAI AUTO / CONTROL ROOM</span><h1>Вход в админ-панель</h1><p>Управление каталогом и контактами компании.</p><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Пароль<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} /></label>{error&&<div className="admin-message">{error}</div>}<button className="admin-primary" disabled={loading}>{loading?'Входим…':'Войти'}</button></form></div>}
